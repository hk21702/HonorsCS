package other;

import other.HCS;
import java.util.*;

public class cc{
    public static void back(int op){
        HCS hi = new HCS();
        int x = op;

        if(x>1)
        HCS.hello();
        else
        HCS.yes();
    }
}